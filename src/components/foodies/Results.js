import React from 'react'
import Trucks from './Trucks'
 
const Results = (props) => {
//    const t= props["trucks"].map((trucks, id) => {
    //    console.log(truck)
        // for (const t of trucks){
          
        //     return <Truck info={t} key={id}/>
            
        // }
        // const renderTexts = this.props.restaurants.map((restaurant, id) => <Restaurant delete={this.props.delete} key={id} restaurant={restaurant}/>)

    // })
    // props.map((t) => console.log(t))
    // const t = props.trucks[0]
    // .map((t) => console.log(t))
    // const t = props.map((t) => console.log(t))
    // console.log(props["trucks"])
//    const t = props.trucks.map((truck, id) => 
//    const t = console.log(props)
//    <Truck name={truck.name} key={id}/>
    // )
  return (
      <ul>
           {/* <div>{t}</div> */}
      </ul>
 
  )
}
 
export default Results